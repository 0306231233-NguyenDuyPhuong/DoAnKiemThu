import 'dart:ui';

class ShopeeColors {
  static const Color primary = Color(0xFFFF5722);     // Cam Shopee
  static const Color primaryLight = Color(0xFFFF8A50); // Cam nhạt
  static const Color background = Color(0xFFF5F5F5);   // Nền
  static const Color white = Color(0xFFFFFFFF);
  static const Color textDark = Color(0xFF212121);     // Chữ đậm
  static const Color textGray = Color(0xFF757575);     // Chữ phụ
}
